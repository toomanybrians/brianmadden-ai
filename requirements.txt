# Python dependencies for brianmadden-ai's pipeline (skills/, scripts/).
# scripts/check_doc_accuracy.py is stdlib-only and needs none of this.
# `requests` also covers the openrouter provider in skills/lib/llm.py
# (OpenAI-compatible HTTP API) — no separate SDK needed for that provider.
anthropic==0.86.0
feedparser==6.0.12
PyYAML==6.0.3
requests==2.32.5
